sudo: required

services:
  - docker

language: node_js
node_js:
  - "node"
  - "8.1.2"

addons:
  sauce_connect: true

script:
  - docker-compose up --build -d
  - npm run lint && npm run type-check && npm run e2e
