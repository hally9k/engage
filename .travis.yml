sudo: required

services:
  - docker

language: node_js
node_js:
  - "node"
  - "latest"

addons:
  sauce_connect: true

script:
  - docker-compose up --build -d
  - npm run lint && npm run type-check && npm run e2e
