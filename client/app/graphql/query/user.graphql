query($id: ID) {
    user(id: $id) {
        id
        firstName
        lastName
        email
        avatar
        children {
            id
            age
            name
            subjects {
                id
                title
            }
        }
    }
}
