query($userId: ID!) {
    conversation(userId: $userId) {
        id
        channel
        messages {
            id
            conversationId
            user {
                firstName
                id
            }
            createdAt
            content
        }
    }
}
