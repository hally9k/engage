query($userId: ID!) {
    conversation(userId: $userId) {
        id
        channel
        messages {
            user {
                firstName
                id
            }
            createdAt
            content
        }
    }
}
